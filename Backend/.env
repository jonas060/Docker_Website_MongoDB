MONGODB_CONNECTION_STRING=mongodb://root:password@mongodb:27017/
# The above connection string is used to connect to the MongoDB instance running in the Docker container.
# It specifies the protocol (mongodb), username (root), password (password), hostname of our mongodb container (mongodb), and port (27017) for the MongoDB connection.